/** @type {import('next').NextConfig} */
const nextConfig = {
  // App directory is enabled by default in Next.js 13+
  // experimental: {
  //   appDir: true, // This is no longer needed
  // },
}

module.exports = nextConfig